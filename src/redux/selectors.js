export const themeSelector = (state) => {
    return state.theme.darkMode
}

export const temporaryFollowed = (state) => {
    return state.temporaryFollowed
}

export const temporaryUnFollowed = (state) => {
    return state.temporaryUnFollowed
}

export const mutedVideo = (state) => {
    return state.mutedVideos
}
